# Email Query Processing Cloud Function Dependencies

# Supabase
supabase==2.0.0

# HTTP client for Query API
httpx==0.25.2

# Email service (shared)
# Note: This should reference the email-ingestion service's src/ directory
# For now, we'll copy the necessary files or use a shared package

# Utilities
python-dateutil==2.8.2
requests==2.31.0
